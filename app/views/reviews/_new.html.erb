<%= simple_form_for [cocktail, review] do |f| %>
  <%= f.input :rating, collection: 1..5 %>
  <%= f.input :comment %>
  <%= f.button :submit, "Add new review", class: "btn btn-warming btn-add" %>
<% end %>

